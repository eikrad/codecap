# Product name is codecap, not agentusage

Agent stays in the glossary as the coding product. The D-Bus name, plasmoid id, and helper binary are codecap (`dev.codecap.Helper`, `/dev/codecap/Helper`, `dev.codecap.plasmoid`) so the package is distinctive and not Claude-branded. Renaming the bus later is painful once the `.service` file is installed.
