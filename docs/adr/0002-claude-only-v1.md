# Claude-only v1; Allowance is optional per Agent

Agent is a first-class concept so a second widget can later bind to a non-Claude Account. v1 implements Claude only, because Five-Hour and Seven-Day Allowance are Claude Pro/Max signals and that is the metric we care about most. Other Agents can ship later with Consumed Usage and no Allowance until the vendor exposes remaining entitlement.
