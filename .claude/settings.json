{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "if": "Bash(gh pr create*)",
            "command": "\"${CLAUDE_PROJECT_DIR:-.}/scripts/pre-pr-check.sh\" >&2 || { echo 'Pre-PR gates failed (see above). Fix them before opening the PR.' >&2; exit 2; }",
            "timeout": 900,
            "statusMessage": "Running the CI gates before opening the PR"
          }
        ]
      }
    ]
  }
}
